现在我们开始进一步规划我们的项目

我们的项目要修改名字，Metric Bot，git上面的项目也同步修改一下

模块规划如下，把规划模块描述优化成一个文档，可以作为我们记忆中的一部分，不用agent 也可以读取，并且标注一下进度

1:  监控面板

主要用于展示当前触发的告警数，历史告警数

2：智能对话,对话参考sqlbot的页面 ，主要是通过对话的形式，使用自然语言进行运维分析

3、智能监控

主要用于展示配置用户定制的告警规则

4：配置

- 模型管理

主要用于配置用户使用哪些大模型

- 监控数据源

主要用于配置监控数据源，有常见的比如Prometheus，zabbix等

- 日志配置

主要用于配置日志数据源，主流的日志数据源比如es、ck、starrocks

- 主机模型

主要用于同步由监控数据生成的主机信息，也可以由接口上报，可以手工录入

- 关系模型

用于配置主键间的关系，可以通过接口自动上报,他通过日志自动发现，也可以通过用户手工配置